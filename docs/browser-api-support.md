# Browser API Support

PageCore is a headless/scraping-oriented engine. Its browser API surface is
therefore intentionally smaller than a general-purpose browser. The goal is to
load modern pages far enough to extract final DOM/HTML/text or render a
screenshot, without advertising APIs that would send libraries down code paths
PageCore cannot execute.

This document describes the page-visible API surface installed by
`src/dom_shim/90_install.js` and related behavior implemented in
`src/dom_shim/`, `src/page.cpp`, and the C++ resource/rendering pipeline.

## Status Policy

- `supported`: The API is page-visible, has a useful implementation for
  PageCore's headless use cases, and is covered by focused tests. This does not
  mean full browser or WPT compatibility.
- `partial`: The API is page-visible and intentionally usable, but has known
  limitations. Feature detection may treat it as available.
- `absent`: The API is intentionally not installed. Page scripts should take a
  fallback path when using feature detection.

When in doubt, prefer `absent` over a no-op or always-failing stub. A false
negative in feature detection is usually safer for scraping than a false
positive.

## Supported

| API surface | Current support | Primary coverage |
| --- | --- | --- |
| `window`, `self`, `top`, `parent`, `Window` identity | Single-window identity, iframe window identity for the lightweight iframe model, and stable `Window.prototype` event methods. | `test_browser_like_web_api_shims`, `test_global_event_listener_aliases_bind_to_window` |
| `DOMException` | DOM-style error object with legacy constants. | `pagecore_dom_shim_unit_tests` |
| `EventTarget`, `Event`, `CustomEvent`, `MessageEvent`, `UIEvent`, `MouseEvent`, `KeyboardEvent`, `PointerEvent`, `FocusEvent`, `HashChangeEvent` | Listener registration/removal, `once`, `signal`, capture/bubble phases, object listeners, handler exceptions reported without aborting later listeners. `document.createEvent()` resolves the legacy interface names PageCore models (ASCII-case-insensitively) and throws `NotSupportedError` otherwise — device-sensor and touch interfaces are deliberately absent rather than stubbed, so feature detection is not misled. | `test_event_options_bubbling_and_wpt_driver_shim`, `test_event_capture_bubble_phases`, `pagecore_dom_shim_unit_tests` |
| `AbortController`, `AbortSignal` | Listener cleanup through `{ signal }`; signal objects are exposed. | `pagecore_dom_shim_unit_tests` |
| DOM node model: `Node`, `CharacterData`, `Text`, `Comment`, `Attr`, `Element`, `Document`, `DocumentFragment` | Lexbor-backed tree, attributes, text, fragments, cloning, sibling/child traversal, stale wrapper invalidation. `CharacterData` sits between `Node` and `Text`/`Comment` and owns the data mutation API (`data`, `length`, `substringData`, `appendData`, `insertData`, `deleteData`, `replaceData`, with WebIDL unsigned-long offsets), plus `Text.splitText()`/`wholeText`. `Text` and `Comment` are constructible (`new Text("x")`). `document.compatMode` reflects the parsed document mode (`BackCompat` for quirks, `CSS1Compat` otherwise). `getRootNode()` returns the true root of a detached subtree (its topmost ancestor), not the document. Node-derived interfaces carry `Symbol.toStringTag`, so `Object.prototype.toString.call(node)` reports the real interface name (e.g. `[object HTMLDocument]`) instead of `[object Object]`. | `test_tree_operations_and_clone`, `test_inner_html_invalidates_stale_wrappers`, `test_deep_dom_traversal_is_iterative`, `test_document_compat_mode_reflects_dom_quirks_mode`, `pagecore_dom_shim_unit_tests` |
| Selectors and traversal | `querySelector`, `querySelectorAll`, `matches`, `closest`, `getElementsBy*`, `TreeWalker`, `NodeIterator`, with compatibility fallbacks for escaped colon classes and `:target`. | `test_query_selector_cache_returns_all_and_first`, `test_escaped_colon_class_selector_fallback`, `test_target_pseudo_class_selector_fallback`, `pagecore_dom_shim_unit_tests` |
| `NodeList`, `HTMLCollection` | Real collection objects rather than plain Arrays. `childNodes`, `children` and `getElementsBy*` are **live** (they re-read the tree on every access) and have stable identity, so `el.childNodes === el.childNodes`. `querySelectorAll` is static, per spec. `HTMLCollection` supports named access by `id`, and by `name` for the elements that expose it. Note they are iterable but, like in a browser, have **no Array methods** — use `[...collection]` or `Array.from()`. | `test_described_traversal_wraps_children_correctly`, `pagecore_dom_shim_unit_tests` |
| DOM mutation helpers | `innerHTML`, `outerHTML`, `insertAdjacent*`, `append`, `prepend`, `before`, `after`, `replaceWith`, `remove`, `Node.normalize()` (merges adjacent text nodes, drops empty ones), fragment parsing, `document.write/open/close`. `appendChild`/`insertBefore`/`replaceChild`/`removeChild` enforce the DOM pre-insertion validity algorithm, throwing `HierarchyRequestError` (bad parent type, inserting a node into itself or a descendant, a non-insertable node, a Text node under a document, a second element child of a document) and `NotFoundError` (reference/removed node is not a child), instead of surfacing a bridge-level `TypeError`. `Element`/`Document`/`DocumentFragment.moveBefore()` additionally enforces "pre-move validity" (a shared `getRootNode({composed: true})`, an Element-or-CharacterData-only node restriction) before delegating to `insertBefore`'s existing move mechanics; it does not exist at all on non-`ParentNode` types, matching spec. No `disconnectedCallback`/`connectedMoveCallback` custom-element reactions fire on any removal or move yet (`disconnectedCallback` is not implemented at all, for any code path). | `test_inner_html_fragment_parsing`, `test_document_write_fragment_insertion`, `test_document_write_external_script_and_open_close`, `test_dom_shim_spec_regressions`, `test_node_move_before` |
| `MutationObserver` | Child-list, attribute, and character-data records, `attributeFilter`, old-value handling, explicit delivery during PageCore's event-loop checkpoint, and delivery participation in page readiness. Re-`observe()`-ing a target replaces that target's options. Registered observers are held weakly (with a strong hold only while records are pending), so an observer dropped without `disconnect()` can be garbage collected instead of leaking for the page's lifetime. | `test_dom_fragment_range_serializer_and_mutation_observer`, `test_text_content_mutation_observer_records_nodes`, `test_mutation_observer_old_value`, `test_event_loop_ordering_contract`, `test_page_readiness_ready_waits_for_timer_fetch_and_dom_stable`, `pagecore_dom_shim_unit_tests` |
| IDL attribute reflection | Content attributes reflect as IDL properties through a type table: `DOMString`, nullable `DOMString`, `boolean`, `long`, `unsigned long`, "limited" (non-negative) variants, URL, and enumerated. Integers follow the HTML parsing rules (leading whitespace and sign, digits, trailing text ignored), not `Number()`. Covers the global attributes (`title`, `lang`, `dir`, `accessKey`, `autofocus`, `tabIndex`, …), the full ARIA surface (`role`, `ariaLabel`, …, reflecting `aria-*` as nullable strings), and per-element attributes for `input`, `button`, `link`, `meta`, `base`, `style`, plus the obsolete `marquee`/`font`/`dir`/`frame`/`frameset` interfaces. | `test_idl_attribute_reflection` |
| `DOMTokenList`, `dataset` / `DOMStringMap` | Class-list and dataset facades backed by Lexbor attributes. `DOMTokenList` is an ordered set (duplicates collapse, so `class="a a"` has length 1) while `value` stays the verbatim attribute text; supports index access, `replace()` with ordered-set semantics, and `[PutForwards=value]` assignment to `classList`. An empty token throws `SyntaxError`, a token with whitespace throws `InvalidCharacterError`. `dataset` is a Proxy whose prototype is `DOMStringMap.prototype`, so `element.dataset instanceof DOMStringMap` holds. | `test_dataset_attributes_and_cached_facades` |
| `Range`, `StaticRange` | `Range` boundary points, `selectNodeContents`, `createContextualFragment`, `getBoundingClientRect`, a real `commonAncestorContainer` and `toString()` (tree-order concatenation of contained text, not `startContainer.textContent`). `cloneContents`/`extractContents`/`deleteContents`/`insertNode`/`surroundContents` implement the DOM Standard's "range extract"/"range clone" algorithms (partially-contained boundary nodes are split via a recursive sub-range, fully-contained nodes are moved or deep-cloned). `compareBoundaryPoints`/`comparePoint`/`isPointInRange`/`intersectsNode` are also implemented. A `Range` does not yet track live mutations (e.g. `insertBefore` elsewhere in the tree does not adjust an existing range's boundary offsets). `StaticRange` is constructible and records boundary points without tracking mutations; `DocumentType`/`Attr` containers throw `InvalidNodeTypeError`. | `pagecore_dom_shim_unit_tests`, `test_range_content_methods` |
| `MutationRecord`, `XMLDocument`, `HTMLDocument`, `DocumentType`, `ProcessingInstruction`, `CDATASection` | Installed as globals so `instanceof` works. `HTMLDocument` is a legacy alias of `Document` (PageCore parses a single HTML document). `document.doctype` returns a real, materialized `DocumentType` node for a parsed `<!DOCTYPE html>`, with `name`/`publicId`/`systemId` all bridge-driven (native `lxb_dom_document_type_public_id`/`system_id` accessors). `DOMImplementation.createDocumentType(qualifiedName, publicId, systemId)` constructs a standalone one natively (`lxb_dom_document_type_create`), validating `qualifiedName` against the XML Name/QName productions (`InvalidCharacterError`/`NamespaceError`) with the same regexes `xml-name-validator`/jsdom use. `DOMImplementation.createDocument(namespace, qualifiedName, doctype)` now returns a working `XMLDocument`: since PageCore has exactly one physical Lexbor document, it is a JS-only container (like `createHTMLDocument()`'s result) whose `createElement`/`createTextNode`/`createComment`/`createDocumentFragment` borrow real bridge-backed nodes from the singleton document without linking them into its tree, and whose own `childNodes`/`appendChild`/`insertBefore`/`removeChild` are a plain array using the same `__fragmentParent` mechanism `DocumentFragment` uses. The bare `new Document()` constructor delegates to `createDocument(null, '')` for the same reason. `Document.createProcessingInstruction(target, data)` works on any document (HTML or XML); `createCDATASection(data)` throws `NotSupportedError` on HTML documents (the real `document` and `createHTMLDocument()`'s result) and only works on an `XMLDocument`, per spec. Both validate their arguments (`InvalidCharacterError` for a bad target or for data containing `"?>"`/`"]]>"`) before creating the native node (`lxb_dom_document_create_processing_instruction`/`create_cdata_section`); neither has a page-script constructor (`new ProcessingInstruction()`/`new CDATASection()` throw, matching the spec, which defines no constructor operation for either). `createHTMLDocument()`'s result (`DetachedHTMLDocument`) has the same kind of real, ordered child list: an implicit `html`-named doctype (built the same native way), `documentElement` folded into that list (so `removeChild(documentElement)` works, matching a real WPT idiom several `dom/ranges/*.html` files use to build a comparison document), and a working `appendChild`/`insertBefore`/`removeChild` sharing the same validity gate. Known gaps: an `XMLDocument`/`new Document()`/`DetachedHTMLDocument` result is not `instanceof Document` (browsers: `XMLDocument`/`HTMLDocument` : `Document`), every node's `ownerDocument` still unconditionally returns the real singleton `document`, never the container that actually created it, and neither container has `compareDocumentPosition()`/`contains()` of its own (several `dom/ranges/*.html` files use a detached document itself, not just its elements, as a Range boundary container). Lexbor's own HTML serializer (used by `outerHTML`/`--format html`) has no case for a `CDATASection` node and errors out if one appears anywhere in the serialized subtree — obscure (it requires deliberately re-parenting a CDATA node from an `XMLDocument` into the real document), and not worth patching Lexbor upstream for now (same category as the existing IDNA/gb18030 gaps). | `pagecore_dom_shim_unit_tests`, `test_node_move_before`, `test_dom_implementation_create_methods` |
| `URL`, `URLSearchParams` | URL parsing/resolution and query parameter operations used by modern JS bundles. `href`/`toString()` serialize embedded `user:password@` credentials; `origin` stays userinfo-free per spec. `URLSearchParams` follows the `application/x-www-form-urlencoded` parser/serializer at the byte level (percent-decoding and UTF-8 decoding never throw; malformed input degrades to a literal `%` or U+FFFD), applies USVString conversion to names/values, and exposes a live, index-based iterator (`forEach`/`entries`/`keys`/`values`) so mutating the list mid-iteration is observed the same way browsers observe it. Hostnames on the six special schemes (`http`, `https`, `ws`, `wss`, `ftp`, `file`) go through IDNA/UTS46 processing and Punycode encoding via a native binding onto Lexbor's `lxb_unicode_idna_to_ascii` (`host.domainToAscii` in `src/js_runtime.cpp`), plus a check for the WHATWG-specific forbidden host code points (space, C0 controls, `` #/:<>?@[\]^| ``, `%`, DELETE) that UTS46 mapping alone does not reject. Other schemes keep an opaque, unprocessed host. The `hostname`/`host` setters fail silently on invalid input (leave the URL unchanged), matching spec; the constructor and `href` setter throw. | `test_browser_like_web_api_shims`, `test_web_shim_crypto_url_input`, `pagecore_dom_shim_unit_tests` |
| `Headers` | Header normalization, append/set/get/iteration. | `pagecore_dom_shim_unit_tests` |
| `TextEncoder`, `TextDecoder`, `TextEncoderStream`, `TextDecoderStream` | `TextEncoder` is UTF-8-only, per spec. `TextDecoder` supports the full WHATWG Encoding Standard label table (legacy multi-byte encodings included — gbk, gb18030, big5, shift_jis, euc-jp, euc-kr, iso-2022-jp, iso-8859-\*, windows-125x, ...) via a native binding onto Lexbor's decode API (`host.textDecoderCreate`/`textDecoderDecode` in `src/js_runtime.cpp`); labels resolving to the WHATWG "replacement" pseudo-encoding are rejected with `RangeError`, matching spec. Streaming `decode()` calls correctly resume a multi-byte sequence split across chunks and detect a BOM split across chunk boundaries. If the native binding is unavailable (isolated JS-shim unit tests with no C++ host bridge), `TextDecoder` falls back to UTF-8-only. `TextEncoderStream`/`TextDecoderStream` are thin `TransformStream` wrappers; the encoder holds back a lone trailing surrogate across chunks so a split astral character reassembles correctly. Legacy-encoding *encoding* (e.g. a non-UTF-8 `<a href>` query using the document's charset, or `<form>` submission) is not implemented — that needs iframe navigation and/or document-charset-aware URL query encoding, both out of scope here. | `test_text_encoder_decoder_utf8_shims`, `test_url_search_params_robustness`, `test_text_decoder_encoding_support`, `pagecore_dom_shim_unit_tests` |
| HTML character-encoding detection (`document.characterSet`/`charset`) | `Page::load_url` resolves the top-level document's byte encoding before parsing (`decode_html_bytes()` in `src/html_encoding.cpp`), following the practically-relevant subset of the WHATWG "determining the character encoding" algorithm: a BOM (UTF-8/UTF-16LE/UTF-16BE) wins unconditionally; otherwise the transport `Content-Type` charset wins if present and valid (native `lxb_html_encoding_content`/`lxb_encoding_data_by_pre_name`), skipping the prescan entirely; otherwise a `<meta charset>`/`<meta http-equiv=content-type>` prescan over the first 1024 bytes (native `lxb_html_encoding_prescan`/`lxb_encoding_prescan_validate`, which also applies the spec's required UTF-16BE/LE→UTF-8 and x-user-defined→windows-1252 overrides — prescan-only, not applied to a real BOM); otherwise UTF-8. The resolved bytes are transcoded to UTF-8 (or passed through unchanged, BOM aside, when already UTF-8) before `DomDocument::parse()`, and the canonical WHATWG label seeds `document.characterSet`/`charset` as a boot-time literal (`LoadOptions::document_character_set`, mirroring how `base_url` seeds `location.href`). `Page::load_html()` called directly (no `load_url`) keeps defaulting to UTF-8, so every pre-existing call site is unaffected. Full chardet-style statistical sniffing when no BOM/transport/meta signal exists is out of scope (defaults to UTF-8, which the spec permits); subresource encoding (external `<script src>`/`<link rel=stylesheet>`), `XMLHttpRequest.overrideMimeType()`, and legacy document-charset-aware URL query encoding are also out of scope (see the `TextDecoder` row below). `DetachedHTMLDocument`/`XMLDocument` (synthetic, JS-only documents from `createHTMLDocument()`/`createDocument()`) always report `UTF-8` regardless of the real document's resolved encoding, per spec ("create a document" fixes their encoding to UTF-8 unconditionally). | `test_document_character_set_defaults_to_utf8_for_load_html`, `test_page_load_url_decodes_meta_charset_to_utf8`, `test_page_load_url_prefers_transport_content_type_over_meta`, `test_page_load_url_detects_bom` |
| `btoa`, `atob` | Browser-compatible Latin-1 base64 behavior backed by the host C++ base64 bridge. | `test_browser_like_web_api_shims`, `pagecore_dom_shim_unit_tests` |
| `crypto.getRandomValues`, `crypto.randomUUID` | Host CSPRNG-backed random values and UUID v4 formatting. | `test_message_channel_and_crypto_shims`, `test_web_shim_crypto_url_input` |
| Basic timers | `setTimeout`, `clearTimeout`, `setInterval`, `clearInterval` are real wall-clock libuv timers on the page's event loop, with the HTML >5-depth 4ms nesting clamp, deterministic one-task-per-turn draining, and logged callback errors. A zero wait budget executes already-queued zero-delay tasks but leaves delayed timers pending. Intervals do not permanently block readiness or idleness. | `test_timers_and_events`, `test_event_loop_ordering_contract`, `test_timer_wait_budget`, `test_zero_wait_runs_ready_tasks_but_keeps_timers_pending`, `test_page_readiness_wait_until_load_skips_timers`, `pagecore_dom_shim_unit_tests` |
| `localStorage`, `sessionStorage`, `Storage` | In-memory storage for a page runtime. | `test_browser_like_web_api_shims` |
| `document.cookie`, `document.domain` | `document.cookie` reads and writes the page-scoped C++ CookieJar shared with document/sub-resource/fetch/XHR loads. Basic `Domain`, `Path`, `Secure`, `HttpOnly`, `Expires`, and `SameSite` handling is implemented; `Max-Age` takes precedence over `Expires` and is clamped to a 400-day maximum; `SameSite=None` requires `Secure`, and unspecified cookies behave as default-Lax for cross-site subresource requests. A cookie `Domain` that is a public suffix is rejected, a `Domain` on an IP-literal host must equal the host, and the same registrable-domain rule feeds the schemeful same-site check (the full Public Suffix List via libpsl). `__Host-`/`__Secure-` name prefixes are enforced case-insensitively, a non-secure origin cannot overwrite/delete a `Secure` cookie of the same name, control characters in cookie name/value are rejected (header-injection defense), and the jar is bounded per-domain/globally with oldest-first eviction. `HttpOnly` cookies are sent on matching requests but hidden from and protected against `document.cookie` overwrite/delete. `document.domain` exposes the current hostname. | `test_document_domain_and_cookie_jar`, `test_shared_cookie_jar_document_scripts_fetch_and_xhr`, `test_cookie_attributes_path_domain_expires_secure_samesite`, `test_cookie_public_suffix_and_injection_rejected`, `test_cookie_domain_rejected_on_ip_host`, `test_cookie_secure_not_overwritten_by_insecure`, `test_cookie_jar_growth_is_bounded`, `test_public_suffix_list_covers_modern_hosting_platforms` |
| `MessageChannel`, `MessagePort` | Basic in-process port messaging. | `test_message_channel_and_crypto_shims` |
| `XMLSerializer`, `DOMParser` | Basic HTML serialization/parsing helpers. | `test_dom_fragment_range_serializer_and_mutation_observer` |
| Form basics | `HTMLFormElement`, common form controls, `ValidityState`, `checkValidity`, `reportValidity`, `FormData` from forms. Form controls honor the HTML "dirty" flags: `input.value`/`input.checked`, `textarea.value`, and `option.selected` are independent of their `defaultValue`/`defaultChecked`/`defaultSelected` content attributes, and `form.reset()` restores the defaults. | `test_html_element_specific_constructors`, `pagecore_dom_shim_unit_tests` |
| `CSS.escape` | CSS identifier escaping. | `pagecore_dom_shim_unit_tests` |

## Partial

| API surface | Current support and limitations | Primary coverage |
| --- | --- | --- |
| Page readiness | `LoadOptions::wait_until` and `Page::run_until_ready()` support `load`, `network-idle`, `dom-stable`, and `ready`. `ready` waits for lifecycle, JS resource tasks, dynamic scripts, image/stylesheet load events, relevant one-shot timer/network/script/resource tasks, PageCore event-loop checkpoints, and a DOM stable window. `dom-stable` drains readiness-relevant tasks due within the stable window before declaring the DOM quiet; it is still not a full `ready` check. Each task step runs one macrotask, drains Promise jobs, delivers MutationObserver records, then drains Promise jobs created by observer callbacks. Waits run on real wall-clock time over the libuv event loop; a provably quiescent loop (no tasks, timers, transfers, or pending frames) is treated as trivially DOM-stable. This is scraping-oriented and not a full browser network-idle model. | `test_event_loop_ordering_contract`, `test_run_until_idle_logs_throwing_task_hook`, `test_page_readiness_wait_until_load_skips_timers`, `test_page_readiness_ready_waits_for_timer_fetch_and_dom_stable`, `test_page_readiness_dom_stable_runs_pending_page_tasks`, `test_page_readiness_image_and_stylesheet_load_events` |
| Static classic scripts | Standard JavaScript MIME types execute in PageCore's deterministic document order. External scripts start fetching immediately through the async transfer engine; sync scripts execute in document order, `defer` and module scripts run after parsing before `DOMContentLoaded`, and `async` scripts run when their fetch lands, with the `load` event waiting for them. Script requests use the shared CookieJar, carry the document URL as referrer, and response `Set-Cookie` updates the jar. Non-standard script types stay inert. | `test_external_script_via_resource_loader`, `test_external_scripts_load_in_document_order`, `test_static_classic_async_defer_scripts_follow_spec_order`, `test_defer_scripts_run_in_order_before_dcl_and_async_holds_load`, `test_shared_cookie_jar_document_scripts_fetch_and_xhr`, `test_non_javascript_script_types_are_not_executed` |
| Dynamic classic scripts | DOM-inserted classic scripts load through `ResourceLoader`, use the shared CookieJar, carry the document URL as referrer by default, execute as event-loop tasks, set `document.currentScript`, dispatch `load`/`error` after the script body, and do not rerun when moved. DOM-created classic scripts expose `async === true` by default; explicit `script.async = false` uses an ordered FIFO queue. `LoadOptions::js_resource_load_policy` and JS resource budgets can reject JS-initiated dynamic script loads without affecting parser-discovered scripts. Dynamic module scripts are not fetched or executed. | `test_dynamic_script_insertion_executes_classic_scripts`, `test_dynamic_module_scripts_are_not_executed`, `test_shared_cookie_jar_document_scripts_fetch_and_xhr`, `test_js_resource_policy_block_all_keeps_parser_scripts`, `test_js_resource_budgets_limit_count_bytes_and_time` |
| Module scripts | Static external and inline module scripts can import relative dependencies and expose `import.meta.url`; `document.currentScript` is `null`. Dynamic module script insertion is intentionally not executed. Import maps, top-level await, dynamic `import()`, and browser-complete module graph timing are not implemented. | `test_module_script_imports_relative_dependencies`, `test_inline_module_uses_document_url_for_relative_imports`, `test_dynamic_module_scripts_are_not_executed` |
| Page lifecycle | `DOMContentLoaded`, `readystatechange`, and `load` are dispatched after PageCore static script execution and before readiness processing. Ordering against all browser task sources, deferred/module scripts, and parser-blocking stylesheet semantics is simplified. | `test_browser_like_web_api_shims`, `test_document_lifecycle_ignores_ready_state_overrides`, `test_static_classic_async_defer_scripts_follow_spec_order`, `test_defer_scripts_run_in_order_before_dcl_and_async_holds_load` |
| Window named element access | `window[id]` exposes document elements by `id`, and `embed`/`form`/`img`/`object` elements by `name`, through live getters that do not overwrite existing real globals. Duplicate-name `HTMLCollection` behavior is not implemented. | `test_window_named_element_access_exposes_document_ids` |
| `fetch`, `Request`, `Response` | Uses the host `ResourceLoader` from an event-loop task, passes method/body/headers/referrer, returns status/statusText/body/final URL and response headers, hides `Set-Cookie`, honors `credentials: omit/same-origin/include` for Cookie/Set-Cookie handling, and participates in page readiness until its promise settles. `Request.referrer` supports `about:client`, URL values, empty string, and `no-referrer`; HTTP transport sanitizes emitted `Referer` to HTTP(S), strips userinfo/fragments, and suppresses non-HTTP(S) referrers. Full referrer-policy processing is not implemented. A `FormData` request body is serialized as `multipart/form-data` with a generated boundary and a matching auto-set `Content-Type` (the string bridge decodes binary file parts to text, so text fields round-trip exactly while binary uploads are lossy). `LoadOptions::js_resource_load_policy` and JS resource budgets can reject all, cross-origin, or over-budget JS-initiated loads. Transfers run truly asynchronously on the event loop, and `AbortSignal` cancels the in-flight transport. No real CORS model, streaming request body, or full redirect modes. | `test_event_loop_ordering_contract`, `test_request_response_fetch_object_shims`, `test_xhr_and_fetch_load_through_resource_loader`, `test_shared_cookie_jar_document_scripts_fetch_and_xhr`, `test_fetch_xhr_credentials_control_cookie_injection`, `test_cookie_attributes_path_domain_expires_secure_samesite`, `test_curl_loader_sends_user_agent_and_sanitized_referer_on_network_paths`, `test_page_readiness_ready_waits_for_timer_fetch_and_dom_stable`, `test_js_resource_policy_same_origin_blocks_cross_origin_loads`, `test_js_resource_budgets_limit_count_bytes_and_time` |
| `XMLHttpRequest` | Uses `ResourceLoader`, supports method/body/headers, sends the document URL as referrer by default after the same HTTP transport sanitization, exposes response text/json, ready-state transitions, status/statusText/responseURL, response header access with `Set-Cookie` hidden, `withCredentials` CookieJar behavior, abort/error/load events, and page-readiness participation while an async send task is pending. A `responseType` of `json` with an unparseable body still loads successfully with `response === null` (it does not turn the load into an `error`); a `FormData` body is serialized as `multipart/form-data`. `LoadOptions::js_resource_load_policy` and JS resource budgets can reject all, cross-origin, or over-budget JS-initiated loads. Async sends run truly asynchronously on the event loop and `abort()` cancels the in-flight transport; synchronous XHR uses the blocking loader path. Progress events, timeout behavior, upload progress, and streaming are limited. | `test_event_loop_ordering_contract`, `test_xhr_and_fetch_load_through_resource_loader`, `test_shared_cookie_jar_document_scripts_fetch_and_xhr`, `test_fetch_xhr_credentials_control_cookie_injection`, `test_curl_loader_sends_user_agent_and_sanitized_referer_on_network_paths`, `test_xhr_event_handler_exceptions_are_reported`, `test_js_resource_policy_block_all_keeps_parser_scripts`, `test_js_resource_budgets_limit_count_bytes_and_time` |
| `Blob`, `File` | Text/arrayBuffer/slice and file metadata are available. Object URLs and `FileReader` are absent. | `test_request_response_fetch_object_shims` |
| Streams | Default readable/writable/transform flows are implemented; BYOB mode is explicitly rejected even though BYOB-related constructors are installed. | `test_streams_writable_controller_and_tee`, `pagecore_dom_shim_unit_tests` |
| `getComputedStyle`, `CSSStyleDeclaration` | Backed by litehtml's cascade over the real Lexbor DOM. A cached value is reused for a node **only when its layout-input digest is unchanged** — a cheap, allocation-free FNV-1a hash folding a conservative superset of every DOM-derived cascade input for the node and its ancestors (tag/id/class/inline-style/selector attributes, ordered sibling context and child-presence at every level, a global stylesheet generation, and the viewport). Identical digest ⇒ identical cascade, so a `class`/stylesheet-driven change (not just inline style) is always seen. Full enumeration still materializes the style object. Returns computed values, not used values; percentages and `auto` may remain authored. `opacity` reads as `1`; CSS custom properties and `var()` are not resolved. CSSOM-only states that litehtml cannot see do not affect rendering. Under budget pressure the single-property path enters an approximate **bounded mode**, but never fabricates a CSS initial value for an element whose cascade was never resolved: the rebuild that trips bounded mode snapshots every element, and an element first read after the trip still gets one exact rebuild (at most one per layout mutation version). | `test_get_computed_style_reads_display_from_stylesheets`, `test_get_computed_style_matches_real_cascade_for_cases_js_engine_got_wrong`, `test_computed_style_property_reuses_digest_across_unrelated_mutation`, `test_computed_style_reacts_to_class_driven_width_change`, `test_computed_style_property_bounded_mode_keeps_stylesheet_display`, `test_computed_style_property_bounded_mode_resolves_element_created_after_trip` |
| CSSOM: `CSSRule`, `CSSStyleRule`, `CSSMediaRule`, `CSSStyleSheet`, `document.styleSheets` | Basic rule/declaration parsing, insertion, deletion, and stylesheet discovery. DOM-connected and render-pipeline stylesheet requests use the shared CookieJar, carry the document URL as referrer, dispatch `load`/`error`, and participate in page readiness where JS-visible. `disabled` and `adoptedStyleSheets` (including a `ShadowRoot`'s) are JS-visible but do not affect litehtml rendering or `getComputedStyle`. A real `<style>` element placed inside shadow content, by contrast, is real DOM and is picked up by litehtml like any `<style>` elsewhere in the document — shadow boundaries are not a CSS scoping mechanism here, so its rules are not scoped to the shadow tree. | `test_cssom_stylesheets_rules_declarations_and_cascade`, `test_cssom_dynamic_sheets_media_disabled_and_adopted`, `test_page_readiness_image_and_stylesheet_load_events`, `test_shared_cookie_jar_render_resources` |
| `CSS.supports` | Basic declaration-shaped checks only; complex condition parsing is not a full CSS parser. | `pagecore_dom_shim_unit_tests` |
| HTML directionality (`getComputedStyle().direction`, `:dir()`) | litehtml has no `direction` CSS property at all, so this is entirely a JS-level implementation of HTML's directionality algorithm: explicit `dir="ltr"/"rtl"`, `dir="auto"` (and an undefined-state `<bdi>`) first-strong-character auto-detection, and inheritance from the nearest ancestor (defaulting to `ltr` at the root). Auto-detection scans descendant text in tree order, skipping `<script>`/`<style>` content and any descendant that establishes its own directionality (a `<bdi>`, or an element with its own defined `dir`) — these checks read the `dir` *content attribute* and use `instanceof HTMLScriptElement`/`HTMLStyleElement`/`HTMLElement`, not tag-name string comparison, so an SVG-namespaced element whose tag name merely collides with an HTML-only interface (`createElementNS(svgNS, 'script')`, whose prototype `applyElementNamespace()` reassigns to a plain `SVGElement`) is correctly not skipped. A `dir="auto"` `<textarea>` or free-text `<input>` (`hidden`/`text`/`search`/`tel`/`url`/`email`/`password`/`submit`/`reset`/`button`) detects from the control's `value` instead, per spec; every other input type has no descendants either way, so it falls through to the same "nothing found → ltr" default. An invalid `dir="..."` value reads as the undefined state (same as no attribute), not an explicit direction. First-strong-character classification uses QuickJS's Unicode property escapes (`\p{Script=Hebrew|Arabic|Syriac|Thaana|Nko}` for strong-RTL, `\p{L}` for strong-LTR) as a pragmatic stand-in for full UAX#9 Bidi_Class classification, which QuickJS's regex engine does not expose. `getComputedStyle().direction` is a JS-computed override (not a litehtml cascade value) that is still enumerable (`in`, `Object.keys`, `cssText`). `:dir(ltr)`/`:dir(rtl)` — which lexbor's selector engine accepts syntactically but never actually matches — is intercepted before reaching the bridge: `querySelector`/`querySelectorAll` run the rest of the selector through lexbor normally and post-filter by directionality (correct only for `:dir()` in the selector's own rightmost compound, not a non-rightmost compound in a combinator chain, and not tracked per-part in a comma-separated list), while `matches()`/`closest()` evaluate it inline per compound so it composes correctly with combinators. Text still renders left-to-right visually — litehtml has no bidi *reordering*/rendering support; only the DOM-visible computed `direction` value and `dir`/`:dir()` matching are implemented. Directionality does not cross a `<slot>` boundary (real slot assignment is not implemented at all — see Shadow DOM below), `unicode-bidi`, and `document.dir` root-specific special-casing are not implemented. | `test_computed_style_direction_reflects_dir_attribute_and_auto_detection`, `test_dir_selector_matches_and_filters_correctly`, `test_bdi_defaults_to_auto_direction`, `test_dir_auto_form_control_uses_value_not_descendants`, `test_dir_auto_ignores_svg_namespaced_script_style_and_bdi`, `pagecore_dom_shim_unit_tests` |
| `queueMicrotask` | Promise-based microtask scheduling through QuickJS jobs. PageCore checkpoints drain Promise jobs, deliver MutationObserver records, then drain Promise jobs created by observer callbacks before the next task. | `test_event_loop_ordering_contract` |
| Geometry APIs | `getBoundingClientRect`, `getClientRects`, `offset*`, `client*`, and `offsetParent` read from litehtml layout. `getClientRects()` returns a `DOMRectList`-compatible object, the root element's `clientWidth`/`clientHeight` reflect the current viewport, and `HTMLInputElement` mirrors browser inline-axis `clientWidth`/`clientLeft` clipping to the content box while `textarea` keeps padding-box clients. A read is **exact whenever a layout for the current version exists or the per-script forced-layout budget allows** (forced JS geometry/computed-style rebuilds load external stylesheets but skip image/font bytes until final rendering). Absolute percentage border-box widths are corrected against the positioned containing block. The first forced layout on an expensive page snapshots **every** element's geometry, so a JS grid library (Isotope/Masonry) that reads each item during load gets real values instead of the whole grid collapsing. Under continued budget pressure a heavy page enters **bounded mode**, an explicitly approximate deadline backstop that never influences the final image: geometry reads return the last exact measurement *as-is* (approximate-nonzero — a known value is never turned into `null`, since JS grid libraries collapse on null/zero geometry; and it is never analytically patched). `nullopt` is returned only when the node is uncached/disconnected; the per-node subtree dirty epoch only labels the value sound vs approximate. The **final render is always exact** and never fed by these read-time caches (see `README.md`). | `test_geometry_box_model_apis_reflect_real_layout`, `test_input_client_geometry_uses_content_box_inline_axis`, `test_geometry_get_client_rects_returns_domrectlist`, `test_root_client_geometry_uses_viewport`, `test_geometry_absolute_percentage_width_uses_positioned_parent`, `test_geometry_reads_load_stylesheets_but_skip_images_until_render`, `test_layout_serialization_materializes_absolute_percentage_width_without_js_measure`, `test_layout_serialization_skips_stale_cached_absolute_width_after_parent_resize`, `test_element_geometry_expensive_document_seeds_snapshot_before_bounded`, `test_element_geometry_bounded_mode_reuses_last_known_after_own_sizing_change`, `test_element_geometry_bounded_mode_reuses_descendant_as_approximate_after_ancestor_mutation`, `test_element_geometry_bounded_mode_reuses_snapshot_for_unmutated_sibling`, `test_element_geometry_bounded_mode_caps_uncached_exact_layouts`, `test_render_never_injects_cross_viewport_measured_width`, `test_render_reflects_settled_dom_regardless_of_read_history` |
| Scroll geometry | A virtual (JS-only) CSSOM View scroll model: no litehtml patch, since litehtml never actually scrolls its laid-out render tree. `scrollWidth`/`scrollHeight` are the union of an element's own padding-box with every descendant's border-box extent (clamped to be at least `clientWidth`/`clientHeight`), cached per layout-mutation version. `scrollTop`/`scrollLeft` are per-element expandos (the same pattern `<input>`/`<textarea>` use for dirty-value tracking), clamped to `[0, scrollWidth/Height - clientWidth/Height]`. `Element.scrollTo`/`scrollBy`/`scroll` accept both the `(x, y)` and `({left, top, behavior})` call shapes; `behavior: 'smooth'` is accepted but applied instantly (no animation). `window.scrollX`/`scrollY`/`pageXOffset`/`pageYOffset`/`scrollTo`/`scrollBy`/`scroll` delegate to `document.documentElement`'s model, matching real browsers in standards mode. `scrollIntoView()` remains a no-op. Known gap: the offset never reaches litehtml's coordinate space, so rendered output and `elementFromPoint`/`elementsFromPoint` (see below) do not react to a nonzero scroll offset. | `test_element_scroll_width_height_reflect_descendant_extent`, `test_element_scroll_top_left_are_stored_and_clamped`, `test_element_scroll_to_by_scroll_normalize_argument_shapes`, `test_window_scroll_delegates_to_document_element` |
| `Range`, `Selection`, `DOMRect`, `DOMRectList` | Useful range boundaries, contextual fragments, selection state, DOMRect values, and DOMRectList-compatible `getClientRects()` results. `Range.getBoundingClientRect()` returns an empty rect, and `Range.getClientRects()` delegates to the selected start container when possible rather than computing independent text-fragment boxes. | `test_dom_fragment_range_serializer_and_mutation_observer`, `test_geometry_get_client_rects_returns_domrectlist` |
| `document.elementFromPoint`, `elementsFromPoint`, `caretPositionFromPoint` | Real coordinate-based hit-testing against litehtml's render tree (`LayoutEngine::elements_at_point`, backed by litehtml's own `get_element_by_point()` — the same algorithm behind its internal `:hover`/click support), not a stub. Walks paint order (positive z-index descending, normal flow, then negative z-index), skips `display:none`/`visibility:hidden` elements, and respects `overflow` clipping; `elementsFromPoint()` returns every overlapping element front-to-back. PageCore's scroll model (see Scroll geometry above) is JS-only and never reaches litehtml's coordinate space, so document and client coordinates still coincide regardless of any element's scroll offset — pass the same `x, y` for both, and a query inside a scrolled container will not account for that scroll. A point query reuses the current-layout/bounded-mode machinery the Geometry APIs row describes, but — unlike a geometry read — does not force a fresh layout once bounded mode has already tripped, returning `null`/`[]` rather than a stale approximate hit. `caretPositionFromPoint()` returns a `CaretPosition` whose `offsetNode` correctly identifies the hit element's first `Text` descendant in tree order (or the element itself if it has none), but `offset` is always `0` — litehtml exposes no per-glyph text metrics for a true character-level offset. `DetachedHTMLDocument`/`XMLDocument` keep returning `null`/`[]` unconditionally: a detached document is never laid out or painted, so there is no render tree to hit-test against. | `test_hit_testing` |
| `test_driver`/`test_driver_internal` (WPT testdriver vendor shim) | `installWptHook()` (`src/dom_shim/40_web.js`) backs the `test_driver_internal` extension point real WPT test files assign into via `/resources/testdriver-vendor.js`. `click(element, coords)` dispatches a real `pointerdown` → `mousedown` → `focus()` → `pointerup` → `mouseup` → `click` sequence with the given `clientX`/`clientY`/`button`, matching what the vendored (verbatim, upstream) `testdriver.js` client computes via real hit-testing (`document.elementsFromPoint`) before calling in. `send_keys(element, keys)` focuses the element, then per character dispatches `keydown` → `keypress` (printable only) → `.value` mutation + `input` → `keyup`; there is no WebDriver special-key-codepoint table (Backspace/Tab/Enter/arrows/etc.), only plain characters. `action_sequence(actionsByInput)` implements only a single `pointer` action source (`pointerMove`/`pointerDown`/`pointerUp`, resolving `pointerMove`'s element `origin` via the same in-view-center math the client uses, `"viewport"`/`"pointer"` origins falling back to raw `x, y`, and firing a synthesized `click` when a `pointerUp` target matches its `pointerDown` target) — `key`/`wheel`/`none` sources are skipped with a console warning rather than thrown, and there is no touch/multi-touch or true multi-source tick synchronization. None of the three methods implement HTML activation behavior (checkbox/radio toggle, form submission, link navigation, label-to-control forwarding, or disabled-control click suppression) — they ship correct *event dispatch*, not full WebDriver default-action semantics. | `test_wpt_driver_click_dispatches_real_event_sequence`, `test_wpt_driver_send_keys_dispatches_keydown_input_keyup`, `test_wpt_driver_action_sequence_pointer_source`, `pagecore_wpt_smoke` (`dom/events/testdriver-click`) |
| `customElements`, `CustomElementRegistry` | Define/get/whenDefined, construction, upgrade, connected callback, and observed attributes for common custom-element bundles. Browser lifecycle ordering is simplified. | `test_custom_elements_registry_shim`, `test_custom_elements_with_private_fields_construct_instances` |
| Shadow DOM: `attachShadow`, `ShadowRoot`, `HTMLSlotElement` | `attachShadow()` creates a real Lexbor element (internally tagged, invisible to page script/CSS) appended into the host, so shadow content is a genuine part of the tree litehtml lays out and paints — `getBoundingClientRect()`, `getComputedStyle()`, and other geometry/cascade reads resolve real values for it instead of an empty/zero placeholder. The host's light-DOM children are never laid out: `<slot>` assignment is still a stub (`assignedNodes()`/`assignedElements()` are always empty), so nothing distributes them into the render. The shadow container and its two internal marker attributes are hidden from `outerHTML`, `innerHTML`, `getAttribute`/`hasAttribute`/`getAttributeNames`/`attributes`, and attribute-selector queries on the host, but `document.querySelector()` can still reach into shadow content from the light tree — full encapsulation is not implemented. `adoptedStyleSheets` remains JS-visible-only (see the CSSOM row above). | `test_shadow_root_and_element_internals_shims`, `test_shadow_root_builds_tree_from_inner_html`, `test_shadow_dom_hides_markers_and_container_from_js`, `test_shadow_dom_paints_real_content_and_hides_light_dom`, `pagecore_dom_shim_unit_tests` |
| `ElementInternals` | Minimal object for custom-element compatibility. | `test_shadow_root_and_element_internals_shims` |
| HTML element constructors | Many `HTML*Element` constructors are installed so `createElement()` and `instanceof` checks work. Only selected element-specific behavior is implemented. Each interface prototype carries `Symbol.toStringTag`, so `Object.prototype.toString.call(el)` reports the real interface name (e.g. `[object HTMLDivElement]`). | `test_html_element_specific_constructors`, `pagecore_dom_shim_unit_tests` |
| `HTMLCanvasElement` | Constructor is installed, but `getContext()` returns `null` and `toDataURL()` returns `data:,`. This is intentional so canvas feature detection can fail cleanly at context acquisition. | Source-level behavior in `src/dom_shim/30_dom.js` |
| `Image` / `HTMLImageElement` | `new Image()` creates an `img`; `src` schedules a ResourceLoader image request through the shared CookieJar, carries the document URL as referrer, sets `complete`, dispatches `load`/`error`, and participates in page readiness. Width/height, `x`/`y`, and `decode()` are minimal; `x`/`y` read the current layout position and return `0` for `display:none`. `naturalWidth`/`naturalHeight` and real decode-state semantics are not implemented. Rendering image decode is handled by the C++ image pipeline and render image requests also use the shared CookieJar/referrer path. | `test_page_readiness_image_and_stylesheet_load_events`, `test_shared_cookie_jar_render_resources`, `test_html_image_element_x_y_reflect_layout_position`; rendering image tests cover the C++ pipeline. |
| Media elements: `HTMLAudioElement`, `HTMLVideoElement`, `Audio` | Element constructors, `play()`, `pause()`, `load()`, `currentTime`, `muted`, and `canPlayType()` stubs exist for compatibility. No real media playback, buffering, metadata, or events. | `pagecore_dom_shim_unit_tests` |
| `HTMLIFrameElement` | Provides a lightweight detached `contentWindow`/`contentDocument` and local `postMessage`. It does not load frame documents from the network or create an isolated browsing context. | `test_browser_like_web_api_shims` |
| `window.postMessage` | Dispatches a same-window `message` event. Cross-origin/window targeting semantics are not implemented. | `test_browser_like_web_api_shims` |
| `navigator` | Exposes `userAgent`, language fields, `onLine`, `hardwareConcurrency`, `javaEnabled()`, and `mediaDevices.getSupportedConstraints()`. No service worker or real media devices. | `test_browser_like_web_api_shims` |
| `navigator.userAgentData` (User-Agent Client Hints) | A well-formed `NavigatorUAData`: `brands`, `mobile`, `platform`, `toJSON()`, and `getHighEntropyValues()` (resolves with `platformVersion`, `architecture`, `bitness`, `model`, `uaFullVersion`, `fullVersionList`, `wow64`). Low-entropy hints are derived from the configured `userAgent` so they stay consistent with `navigator.userAgent`; the high-entropy values are plausible placeholders, not a real platform probe. Exposed unconditionally (not gated on secure context) so sites that unconditionally call `getHighEntropyValues()` take the normal path instead of an unhandled rejection. | `test_navigator_user_agent_data` |
| `location`, `document.location`, `history` | URL object helpers and minimal `pushState`/`replaceState` mutation of `location`. No navigation, reload, session history stack, `popstate`, or `hashchange` model. | `test_document_location_aliases_window_location`, `test_browser_like_web_api_shims` |
| Viewport/screen globals | `innerWidth`, `innerHeight`, `outerWidth`, `outerHeight`, `devicePixelRatio`, and `screen.*` reflect the most recent render viewport. `window.scroll*` delegate to the virtual scroll model (see Scroll geometry above), which does not move anything litehtml renders. No visual viewport or multi-screen model. | `test_window_viewport_reflects_last_render_options`, `test_browser_like_web_api_shims`, `test_window_scroll_delegates_to_document_element` |
| `matchMedia`, `MediaQueryList`, `MediaQueryListEvent` | `matchMedia()` returns an EventTarget-compatible `MediaQueryList` with `media`, live `matches`, `onchange`, `addListener()`/`removeListener()`, and manual `change` event dispatch support through `MediaQueryListEvent`. Matching covers `all`/`screen`/`print`, empty queries, simple width/height/aspect-ratio features, `all and (...)` serialization, and obvious invalid syntax as `not all`; it does not implement full CSS media-query grammar or fire viewport-change events. | `test_browser_like_web_api_shims`, `test_media_query_list_event_target_shims` |
| `requestAnimationFrame` | A real ~16 ms frame cadence on the libuv event loop: callbacks batch per frame, run once with a monotonic timestamp, and callbacks registered during a frame run in the next frame; `cancelAnimationFrame` works. rAF never blocks readiness by itself, but DOM mutations made in frames extend the stability window. There is still no paint/compositor pipeline behind the frames. | `test_request_animation_frame_real_frames`, `test_animation_frame_loop_does_not_block_idle_or_ready` |
| `requestIdleCallback` | Implemented as a timer callback with `didTimeout:false` and `timeRemaining() === 0`. | Source-level behavior in `src/dom_shim/40_web.js` |
| `performance` | `performance.now()` is the event loop's real monotonic clock (milliseconds since runtime creation); `timeOrigin` is set. Marks, measures, and entries are no-op/empty. | `test_request_animation_frame_real_frames` |
| `Intl` fallback | Missing Intl constructors are filled with simple fallbacks for common library compatibility. Locale formatting is not browser/ICU-complete. | `pagecore_dom_shim_unit_tests` |
| SVG DOM constructors | Common `SVG*Element` constructors exist for `createElementNS()`/`instanceof` compatibility. SVG rasterization is handled separately by the C++ image decoder via [lunasvg](https://github.com/sammycage/lunasvg), covering nearly all of SVG 1.1/1.2 Tiny (shapes, paths, `linearGradient`/`radialGradient`, `pattern`, `clipPath`, `mask`, `marker`, `<use>`/`<defs>`/`<symbol>`, `<style>` CSS, embedded `<image>`, partial `<text>`/`<tspan>`; no animation/filters/scripts), and can be disabled at build time. | `test_create_element_ns_and_template_content_clone`, image decoder/render tests |

## Absent

These APIs are intentionally not installed in the page-visible global object.
Feature detection should fail and libraries should use fallback code paths.

| API surface | Reason |
| --- | --- |
| `Worker`, `SharedWorker`, `navigator.serviceWorker`, Service Worker APIs | No worker runtime, worker event loop, script isolation, cache integration, or service-worker-controlled fetch pipeline. |
| `IntersectionObserver`, `ResizeObserver`, `PerformanceObserver` | No browser observation loop. Geometry can be read synchronously through partial geometry APIs instead. |
| `WebSocket`, `EventSource` | No persistent browser network channel model. Add only if scraping targets require live push data. |
| IndexedDB | No transactional persistent browser database. |
| Cache API | No Service Worker/CacheStorage model. |
| `BroadcastChannel`, `MessageChannel` alternatives across browsing contexts | Only local `MessageChannel` is implemented. |
| `FileReader`, `URL.createObjectURL`, `URL.revokeObjectURL` | Blob URLs and asynchronous file readers are not implemented. |
| Canvas rendering contexts: `CanvasRenderingContext2D`, `OffscreenCanvas`, WebGL contexts | `HTMLCanvasElement.getContext()` returns `null`; no canvas drawing backend is exposed to JS. |
| Real media capture/playback APIs: `getUserMedia`, `MediaStream`, WebRTC, WebAudio | Not useful for the default scraping pipeline and would require large runtime subsystems. |
| Permissions, Notifications, Clipboard, Geolocation, Payment Request | Browser integration APIs are outside the scraping-oriented runtime. |
| Touch-specific event constructors such as `TouchEvent` | Pointer/mouse/keyboard constructor shims exist; touch input is not modeled. |
| CSP and Trusted Types enforcement | PageCore does not implement a browser security policy engine. |
| Visual Viewport API (`window.visualViewport`) | No pinch-zoom/visual-vs-layout-viewport model; see Scroll geometry above for the (virtual, JS-only) layout-viewport scroll state that does exist. |

## Maintenance Rules

- Any change to `src/dom_shim/90_install.js` that adds or removes a
  page-visible global must update this document.
- Any change that turns a `partial` API into a more complete implementation must
  add or update tests and move the entry only when the documented behavior is
  covered.
- Any newly added compatibility stub must be classified deliberately. If the API
  cannot provide useful behavior for headless scraping, leave it absent.
- Keep `README.md` high-level. Use this document for detailed API status and
  limitations.
